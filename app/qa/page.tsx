'use client';
import { useState } from 'react';
const checks = ['Feed works','Play works','Clean works','Sleep works','Shop buy works','Expensive purchase confirmation works','Inventory use works','Inventory filter works','Daily reward works','Minigame reward works','Tournament reward works','Export save works','Import save works','Backup restore works','Migration works','Revive works','Egg hatch works','Errand claim works','Crafting works','Skill tree spend works','News read state works','Remote event fallback works','Cloud sync disabled gracefully without Firebase','Couple Mode permission confirmation works'];
export default function QaPage(){const [done,setDone]=useState<string[]>([]);return <main className="min-h-screen bg-cyan-100 p-4 font-mono"><div className="mx-auto max-w-3xl border-4 border-black bg-white p-4"><h1 className="text-xl font-bold">Pixel Paws v2.0 Manual QA</h1><p className="my-3 text-sm">Hidden checklist for maintainers. State is intentionally session-local.</p><div className="grid gap-2">{checks.map((item)=><label key={item} className="flex gap-3 border-2 border-black p-3"><input type="checkbox" checked={done.includes(item)} onChange={()=>setDone((current)=>current.includes(item)?current.filter((x)=>x!==item):[...current,item])}/><span>{item}</span></label>)}</div><p className="mt-4 border-2 border-black bg-lime-100 p-3">{done.length}/{checks.length} checked</p></div></main>}
