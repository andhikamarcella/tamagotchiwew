'use client';
import { CHANGELOG, GAME_VERSION } from '@/src/config/gameMeta';
export const LAST_SEEN_VERSION_KEY = 'pixel-paws-last-seen-version';
export default function ChangelogModal({ onClose }: { onClose: () => void }) { return <div className="fixed inset-0 z-50 grid place-items-end bg-black/50 p-0 sm:place-items-center sm:p-4"><section className="pixel-border max-h-[85vh] w-full overflow-y-auto rounded-t-2xl bg-white p-4 text-slate-950 sm:max-w-lg sm:rounded-none"><h2 className="mb-2 text-sm">What&apos;s new in v{GAME_VERSION}</h2><div className="grid gap-3">{CHANGELOG.map((item) => <article key={item.version} className="border-2 border-slate-950 bg-lime-50 p-3"><h3 className="text-[11px]">{item.title} · {item.version}</h3><p className="mt-1 text-[9px] text-slate-600">{item.date}</p><ul className="mt-2 list-disc pl-5 text-[10px] leading-relaxed">{item.changes.map((change) => <li key={change}>{change}</li>)}</ul></article>)}</div><button type="button" onClick={onClose} className="pixel-border-sm mt-4 w-full bg-pink-200 px-3 py-2 text-[10px]">Got it</button></section></div>; }
